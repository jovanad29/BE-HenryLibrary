//definir la ruta de la api hacia el controlador de paymentMethod
const { Router } = require('express');
const router = Router();
// requerir los metodos del controlador de paymentMethod

// definir los metodos de la ruta de la api hacia el controlador de paymentMethod


//exportar el router para poder usarlo en el index.js
module.exports = router;